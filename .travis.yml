language: python
python:
  - "3.5"
  - "3.6"
  - "nightly"

# command to install dependencies
install: "pip install -r dev-requirements.txt"

# validates coding style
script: "flake8 --statistics --verbose I1820/"
